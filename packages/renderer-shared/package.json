{
    "name": "@canvas-tile-engine/renderer-shared",
    "version": "0.0.0",
    "private": true,
    "description": "Internal shared renderer modules. Not published: consumers bundle this source via tsup (noExternal).",
    "author": "Enes Yüksel",
    "license": "MIT",
    "sideEffects": false,
    "exports": {
        "./canvas2d": "./src/canvas2d/index.ts",
        "./dom": "./src/dom/index.ts"
    },
    "scripts": {
        "typecheck": "tsc --noEmit",
        "lint": "oxlint",
        "test": "vitest",
        "test:coverage": "vitest run --coverage",
        "format": "oxfmt",
        "format:check": "oxfmt --check"
    },
    "devDependencies": {
        "@canvas-tile-engine/core": "workspace:^"
    }
}
